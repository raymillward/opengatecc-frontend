OpenGateCC Javascript Frontend
============

A javascript single page app to display the information in the opengatecc wordpress site.

It's built using Angular, Bootstrap and Less.

### Setup

1. Install NPM
2. Install bower and gulp using `npm install -g bower gulp`
3. From the root of the repository run `bower install`
4. From the root of the repository run `gulp serve`